import { StyledComponentProps } from 'styled-components';
import { BoxProps } from '../../Box';
export declare type CheckboxProps = StyledComponentProps<'input', any, {
    label: string;
    error?: Boolean;
} & BoxProps, never>;
export declare const Checkbox: any;
